#pragma once
#include <iostream>
#include <string>
#include <fstream>
using namespace std;

//파일 스트림 부모 클래스
class FileManager {
public:
	void fileSave(string fileName) {
	}

	void fileLoad(string fileName) {
	}
};